//mise en page de mon interface admin
<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<VBox alignment="TOP_CENTER" spacing="15.0" xmlns="http://javafx.com/javafx/17.0.12" xmlns:fx="http://javafx.com/fxml/1" fx:controller="controller.AdminController">

    <padding>
        <Insets bottom="20.0" left="20.0" right="20.0" top="20.0" />
    </padding>

    <Label text="Administration Snake" textFill="#2c3e50">
        <font>
            <Font name="System Bold" size="24.0" />
        </font>
    </Label>

    <TableView fx:id="tableUsers" prefHeight="300.0" prefWidth="750.0">
        <columns>
            <TableColumn fx:id="colNom" prefWidth="100.0" text="Nom" />
            <TableColumn fx:id="colPrenom" prefWidth="100.0" text="Prénom" />
            <TableColumn fx:id="colMail" prefWidth="150.0" text="Email" />
            <TableColumn fx:id="colRole" prefWidth="80.0" text="Rôle" />
            <TableColumn fx:id="colScore" prefWidth="100.0" text="Best Score" />
            <!-- Nouvelle colonne pour le dernier score -->
            <TableColumn fx:id="colDernierScore" prefWidth="100.0" text="Last Score" />
        </columns>
    </TableView>

    <Separator prefWidth="200.0" />

    <Label text="Gestion Joueur (Sélectionnez pour modifier)" textFill="#7f8c8d">
        <font><Font name="System Bold" size="14.0" /></font>
    </Label>

    <HBox alignment="CENTER" spacing="10.0">
        <TextField fx:id="txtNom" promptText="Nom" />
        <TextField fx:id="txtPrenom" promptText="Prénom" />
        <TextField fx:id="txtMail" promptText="Email" />
    </HBox>

    <HBox alignment="CENTER" spacing="10.0">
        <PasswordField fx:id="txtMdp" promptText="Mot de passe" />
        <TextField fx:id="txtRole" promptText="Rôle (ex: joueur)" />
    </HBox>

    <HBox alignment="CENTER" spacing="15.0">
        <Button onAction="#handleAjouter" style="-fx-background-color: #3498db; -fx-text-fill: white;" text="Ajouter" />
        <!-- Nouveau bouton Modifier -->
        <Button onAction="#handleModifier" style="-fx-background-color: #f39c12; -fx-text-fill: white;" text="Modifier" />
        <Button onAction="#handleSupprimer" style="-fx-background-color: #e74c3c; -fx-text-fill: white;" text="Supprimer" />

        <Button onAction="#handleLancerJeu" style="-fx-background-color: #27ae60; -fx-text-fill: white; -fx-font-weight: bold;" text="▶ JOUER" />
    </HBox>
   <Button fx:id="deconnexion" mnemonicParsing="false" style="-fx-background-color: red;" text="Deconnexion" textFill="WHITE" onAction="#deconnexion"/>

</VBox>
